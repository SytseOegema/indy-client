using System;

namespace indyClient
{
    class CredDefFacilitator
    {
        public string generateCredValueJson(string attributes, string values)
        {
            Console.WriteLine(attributes);
            Console.WriteLine(values);
            return "";
        }

    }
}
